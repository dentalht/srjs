'use strict';

describe('srApp.faq module', function() {

  beforeEach(module('srApp.faq'));

  describe('faq controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var faqCtrl = $controller('FaqCtrl');
      expect(faqCtrl).toBeDefined();
    }));

  });
});