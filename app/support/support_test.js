'use strict';

describe('srApp.support module', function() {

  beforeEach(module('srApp.support'));

  describe('support controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var supportCtrl = $controller('SupportCtrl');
      expect(supportCtrl).toBeDefined();
    }));

  });
});